//instantiates the game and specifies the configurations
var gameConfiguration =
{
  type : Phaser.AUTO,
  width : 800,
  height : 600,
  scene : [MainMenu]
};
var game  = new Phaser.Game(gameConfiguration);
